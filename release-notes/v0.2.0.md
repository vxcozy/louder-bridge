# Louder Bridge 0.2.0

This prerelease adds Codex Micro support for local sessions in Hermes Desktop.

## What works

- Hold MIC to speak in the Hermes composer, then release it to stop.
- Use the key beside MIC to send the draft.
- Use the six Agent Keys to open their assigned Hermes sessions.
- See session state on the keys and exterior lighting.
- Move between Hermes Desktop, Claude Desktop, and Codex without restarting
  Louder Bridge. The bridge waits when more than one supported app is open so
  the Micro has one owner.

## What changed

Setup now installs a small Hermes plugin that reports lifecycle events to the
local bridge. Voice input, sending, and session navigation use Hermes Desktop's
local controls. The first MIC press briefly retries while Hermes exposes its
composer to macOS Accessibility.

Claude Desktop support now shares the same native voice helper and keeps its
existing behavior.

## Hardware result

A locally built v0.2.0 candidate archive passed a focused physical test on
August 4, 2026:

| Component | Version or result |
|---|---|
| macOS | 26.5.2 (25F84) |
| Hermes Desktop | 0.17.0 |
| Hermes Agent | 0.19.0 (2026.7.20), upstream `a4973c3f` |
| Embedded Node.js | 22.23.1 |
| Codex Micro firmware | v0.4.1 |
| Connection | Bluetooth Low Energy |
| Voice input | Three MIC hold-and-release cycles passed |
| Send key | Passed |
| Agent Keys | Session navigation passed |
| Lifecycle response | Lighting, exterior effects, and sound feedback passed |
| Installation | Fresh app and background-agent upgrade passed after permission approval |
| App handoff | Codex released the Micro to Hermes and regained it afterward |

The tester confirmed that dictated text appeared in the Hermes composer before
using the send key. The installed candidate test had no missed controls or
rough transitions.

## Compatibility

- Apple Silicon
- macOS 15 or newer
- Hermes Desktop with local sessions
- Claude Desktop with local Claude Code workspaces
- Codex Micro firmware v0.4.1

## Privacy

Louder Bridge runs locally. It collects no telemetry, audio, transcripts,
prompt content, project names, or session identifiers. Its local diagnostics
require a private authentication token.

## Download

The release contains an Apple Silicon ZIP, its SHA-256 checksum, and an SPDX
SBOM. The app uses an ad-hoc signature, so macOS may require **Open Anyway** in
**System Settings → Privacy & Security** on first launch.

Install **Louder Bridge.app** in **Applications**, open it once, and approve
Input Monitoring and Accessibility. Normal use does not require Terminal.

This is a prerelease, not the stable v1 release. Hermes integration uses local
plugin, shortcut, and Accessibility interfaces that may change in later Hermes
versions.
